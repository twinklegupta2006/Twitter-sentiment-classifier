numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
tensorflow==2.14.0
matplotlib==3.8.4
pyyaml==6.0.1
pytest==8.2.0
jupyter==1.0.0
streamlit==1.35.0
# Pinned explicitly: streamlit>=1.36 requires protobuf>=5.26, which conflicts
# with tensorflow==2.14.0's protobuf<5 requirement. streamlit==1.35.0 is the
# newest version whose own protobuf constraint (<5) is compatible with TF.
protobuf==4.25.9
